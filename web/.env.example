# The deployed API base URL (AWS Lambda function URL or API Gateway stage).
# The frontend calls the backend by this URL, never a relative path -- they deploy
# separately. Must be NEXT_PUBLIC_ to be readable in the browser.
NEXT_PUBLIC_API_URL=https://your-lambda-url.lambda-url.us-east-1.on.aws
